# Aether - Swarm/Portainer production environment
# Copy values into Portainer stack environment variables.

# Required domain and image namespace
DOMAIN=app.example.com
DOCKERHUB_USERNAME=yourdockerhubuser

# Optional: single DB URL for both dashboard and orchestrator.
# If unset, docker-compose.prod.yml falls back to internal postgres service.
DATABASE_URL=

# Security-sensitive secrets (generate strong random values)
POSTGRES_PASSWORD=replace_with_strong_password
BETTER_AUTH_SECRET=replace_with_long_random_secret_min_32_chars
AGENT_SECRET=replace_with_random_agent_secret

# Orchestrator-created agent containers must join this exact overlay network name.
# Keep this in sync with docker-compose.prod.yml -> networks.aether_network.name
AGENT_NETWORK=aether_network

# Optional shared model cache on VPS host
AGENT_SHARED_MODELS_HOST_PATH=/opt/aether/models
AGENT_SHARED_MODELS_ORCH_PATH=/shared-models
AGENT_SHARED_MODELS_CONTAINER_PATH=/models

# Optional provider keys (system fallback; users can still set per-account keys)
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
SARVAM_API_KEY=

# Optional OAuth providers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Optional runtime tuning
AGENT_IDLE_TIMEOUT=30
AETHER_VAD_MODE_DEFAULT=off
AETHER_TURN_DETECTION_MODE_DEFAULT=off
