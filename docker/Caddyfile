aether.suryaumapathy.in {
    # API routes to orchestrator
    @api path /api/*
    
    # API goes to orchestrator
    reverse_proxy @api localhost:4000
    
    # Everything else to dashboard
    reverse_proxy localhost:3000
    
    # WebSocket/SSE handled by reverse_proxy
}
